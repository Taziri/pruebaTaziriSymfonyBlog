nelmio_api_doc:
    models: { use_jms: false }
    documentation:
        info:
            title: Test Blog
            description: swagger for TestBlog
            version: 0.0.1
    areas: # to filter documented areas
        path_patterns: # an array of regexps (document only routes under /api, except /api/doc)
            - ^/api(?!/doc$)