nelmio_api_doc:
    documentation:
        info:
            title: Test Blog
            description: swagger para la api de la prueba de blog
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
